# Copy to .env. Real .env is gitignored.

DOMAIN=lms.infinitynexatech.com

# DB (generate with: openssl rand -hex 24)
DB_ROOT_PASSWORD=
DB_PASSWORD=

# Admin (generate with: openssl rand -hex 16)
ADMIN_PASSWORD=
ADMIN_EMAIL=admin@infinitynexatech.com
