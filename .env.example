# Copy to .env. Real .env is gitignored.

DOMAIN=lms.infinitynexatech.com
SUPPORT_EMAIL=admin@infinitynexatech.com

# DB
DB_USER=bn_moodle
DB_NAME=bitnami_moodle
DB_PASSWORD=                 # openssl rand -hex 24

# Admin (created on first boot in dev; in prod use admin/cli/install.php instead)
ADMIN_USER=admin
ADMIN_PASSWORD=              # openssl rand -hex 16
ADMIN_EMAIL=admin@infinitynexatech.com

# Site
MOODLE_SITE_NAME=Infinity Learn
